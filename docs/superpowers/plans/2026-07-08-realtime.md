# Realtime — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que los dos miembros de la pareja vean los cambios del otro en vivo, sin navegar ni refrescar.

**Architecture:** Un trigger en las 6 tablas compartidas emite un aviso a un canal **privado por pareja**
(`couple:<couple_id>`) con `realtime.send(..., private := true)`. Una policy en `realtime.messages` deja
escuchar solo a esa pareja. El navegador no lee el payload para pintar: solo llama a `router.refresh()`, porque
esta app no tiene estado en el cliente (todo es Server Components + `revalidatePath`).

**Tech Stack:** Supabase Realtime (Broadcast desde la base de datos), plpgsql, `@supabase/ssr`
(`createBrowserClient`), Next.js 15 App Router, React 19.

**Spec:** `docs/superpowers/specs/2026-07-08-realtime-design.md`

## Global Constraints

- **No hay test runner en este repo.** No hay jest ni vitest; `playwright` está instalado pero no hay suite.
  **No se puede hacer TDD.** La verificación por tarea es build verde (incluye `tsc`) y, en la Task 1, un smoke
  test real contra la base de datos. **No añadas un test runner. No escribas archivos de test.**
- **Comando de build (exacto):** `NODE_OPTIONS=--use-system-ca corepack pnpm build`
  (proxy TLS corporativo; `pnpm` no está en el PATH → hay que pasar por `corepack`).
- **Las migraciones las aplica el controlador vía Supabase MCP**, no un subagente: solo el controlador tiene ese
  acceso. Un subagente que necesite aplicar SQL está BLOCKED; que lo diga.
- **Firmas reales, introspeccionadas del proyecto (no las cambies):**
  `realtime.send(payload jsonb, event text, topic text, private boolean) returns void` ·
  `realtime.topic() returns text` · `public.get_my_couple_id() returns uuid` (`STABLE SECURITY DEFINER`).
- **En un trigger de `DELETE`, `NEW` no está asignado.** Leer `new.couple_id` lanza en tiempo de ejecución. Hay
  que ramificar por `tg_op`. Esta es la razón por la que este diseño existe: el trigger ve `OLD.couple_id`
  entero, mientras que el WAL solo lleva la PK y **los `DELETE` de Postgres Changes no respetan la RLS**.
- **`set search_path = ''` en la función trigger** → todo va cualificado: `realtime.send`, `auth.uid()`,
  `pg_catalog.jsonb_build_object`.
- **Realtime es aditivo, nunca una dependencia.** Si el canal no llega a `SUBSCRIBED`, la app debe seguir
  funcionando exactamente como hoy (revalidación al navegar). Los errores se loguean con `console.error`; no se
  muestran al usuario. No introduzcas ningún estado de UI para "conectado" o "desconectado".
- **El cliente no lee el payload para pintar nada.** Solo `router.refresh()`.
- Comentarios de código en inglés. No hay copy de UI nuevo en esta entrega.

---

## Estructura de archivos

| Archivo | Responsabilidad |
|---|---|
| `supabase/migrations/20260708_realtime_couple_broadcast.sql` (nuevo) | La función trigger, los 6 triggers, la policy de `realtime.messages`, y su rollback comentado. |
| `src/components/realtime-refresher.tsx` (nuevo) | Client Component sin UI. Suscribe al canal privado y llama a `router.refresh()`. |
| `src/app/(app)/layout.tsx` | Lo monta con el `coupleId` y el `userId` que ya tiene. |

---

### Task 1: La migración y su smoke test (la ejecuta el controlador, no un subagente)

**Files:**
- Create: `supabase/migrations/20260708_realtime_couple_broadcast.sql`

**Interfaces:**
- Consumes: `realtime.send`, `realtime.topic`, `public.get_my_couple_id` (existen, firmas arriba).
- Produces: el canal `couple:<couple_id>` emite un evento `cambio` en cada INSERT/UPDATE/DELETE de las 6 tablas,
  y solo los miembros de esa pareja pueden escucharlo. La Task 2 depende de eso.

> **Esta tarea existe para des-riesgar antes de escribir UI.** El spec lo dice: `realtime.send()` desde un
> trigger a un canal privado es la parte con menos control. Si no funciona, nos enteramos aquí y no después de
> construir el componente. La alternativa documentada sería `realtime.broadcast_changes()`.

- [ ] **Step 1: Escribir el archivo de migración**

Crea `supabase/migrations/20260708_realtime_couple_broadcast.sql`:

```sql
-- Realtime: every write to a shared table nudges that couple's private channel.
-- The client does not read the payload; it just refetches. See
-- docs/superpowers/specs/2026-07-08-realtime-design.md
--
-- Why a trigger and not Postgres Changes: RLS is not applied to DELETE in logical
-- replication (the WAL old-record carries only the primary key), so every couple's
-- browser would be woken by every other couple's delete. A trigger sees the whole
-- OLD row, including couple_id.

create or replace function public.notify_couple_change()
returns trigger
language plpgsql
security definer
set search_path = ''
as $$
declare
  cid uuid;
begin
  -- In a DELETE trigger NEW is unassigned; reading new.couple_id raises.
  if tg_op = 'DELETE' then
    cid := old.couple_id;
  else
    cid := new.couple_id;
  end if;

  if cid is not null then
    perform realtime.send(
      pg_catalog.jsonb_build_object('tabla', tg_table_name, 'op', tg_op, 'actor', auth.uid()),
      'cambio',
      'couple:' || cid::text,
      true  -- private channel
    );
  end if;

  return null;  -- AFTER trigger: the return value is ignored
end;
$$;

create trigger notify_couple_change_ai_messages
  after insert or update or delete on public.ai_messages
  for each row execute function public.notify_couple_change();

create trigger notify_couple_change_moods
  after insert or update or delete on public.moods
  for each row execute function public.notify_couple_change();

create trigger notify_couple_change_events
  after insert or update or delete on public.events
  for each row execute function public.notify_couple_change();

create trigger notify_couple_change_expenses
  after insert or update or delete on public.expenses
  for each row execute function public.notify_couple_change();

create trigger notify_couple_change_budgets
  after insert or update or delete on public.budgets
  for each row execute function public.notify_couple_change();

create trigger notify_couple_change_date_ideas
  after insert or update or delete on public.date_ideas
  for each row execute function public.notify_couple_change();

-- Without this policy private channels deny everyone: there is no realtime AND no
-- privacy leak. With it, exactly one couple can read its own topic. This policy IS
-- the access control for the feature.
create policy "couple listens to its own channel"
  on realtime.messages
  for select
  to authenticated
  using (
    extension = 'broadcast'
    and realtime.topic() = 'couple:' || public.get_my_couple_id()::text
  );

-- Rollback:
--   drop policy "couple listens to its own channel" on realtime.messages;
--   drop trigger notify_couple_change_date_ideas on public.date_ideas;
--   drop trigger notify_couple_change_budgets on public.budgets;
--   drop trigger notify_couple_change_expenses on public.expenses;
--   drop trigger notify_couple_change_events on public.events;
--   drop trigger notify_couple_change_moods on public.moods;
--   drop trigger notify_couple_change_ai_messages on public.ai_messages;
--   drop function public.notify_couple_change();
```

- [ ] **Step 2: Aplicarla al proyecto**

Vía Supabase MCP (`apply_migration`), proyecto `iymibuwzwxzcpybcpkrp`, nombre
`realtime_couple_broadcast`. Después, comprobar que existen: la función, los **6** triggers, y **1** policy en
`realtime.messages` (antes había 0).

- [ ] **Step 3: Sembrar DOS parejas desechables**

La BD tiene **una pareja real con datos**. No sembrar ahí. Crear por SQL (MCP) dos parejas desechables, cada una
con un usuario confirmado (`email_confirmed_at`, más su fila en `auth.identities`, o el login falla). Llámalas
`realtime-a@example.com` y `realtime-b@example.com`. Guardar sus `couple_id`.

- [ ] **Step 4: El smoke test — ¿emite de verdad, y solo a quien debe?**

Escribe `pw-smoke.mjs` en la raíz del repo (ahí resuelve `node_modules`; `pw-*.mjs` está gitignorado):

```js
import { createClient } from "@supabase/supabase-js";

const URL = "https://iymibuwzwxzcpybcpkrp.supabase.co";
const KEY = "sb_publishable_RNvP6mcAX8rD2Gc3T6GHRQ_7K_scBe_";
const COUPLE_A = process.env.COUPLE_A; // el couple_id de la pareja A

const mk = async (email) => {
  const c = createClient(URL, KEY);
  const { error } = await c.auth.signInWithPassword({ email, password: "SddTest!2026" });
  if (error) throw error;
  return c; // signIn dispara SIGNED_IN -> supabase-js llama a realtime.setAuth()
};

const listen = (c, topic, label) =>
  new Promise((resolve) => {
    const got = [];
    const ch = c
      .channel(topic, { config: { private: true } })
      .on("broadcast", { event: "cambio" }, (m) => got.push(m.payload));
    ch.subscribe((status, err) => console.log(`[${label}] ${status}`, err?.message ?? ""));
    setTimeout(() => resolve({ got, ch }), 9000);
  });

const a = await mk("realtime-a@example.com");
const b = await mk("realtime-b@example.com");

// A escucha su propio canal. B intenta escuchar el canal de A.
const pa = listen(a, `couple:${COUPLE_A}`, "A en su canal");
const pb = listen(b, `couple:${COUPLE_A}`, "B en el canal de A");

console.log(">>> ahora inserta una fila para la pareja A (el controlador lo hace por MCP)");
const [ra, rb] = await Promise.all([pa, pb]);
console.log("A recibió:", JSON.stringify(ra.got));
console.log("B recibió:", JSON.stringify(rb.got));
console.log(ra.got.length >= 1 ? "PASS  A recibe su evento" : "FAIL  A no recibió nada");
console.log(rb.got.length === 0 ? "PASS  B NO recibe el canal ajeno" : "FAIL  FUGA: B recibió el canal de A");
process.exit(0);
```

Córrelo con `COUPLE_A=<uuid> NODE_OPTIONS=--use-system-ca node pw-smoke.mjs` y, mientras espera, inserta por MCP
un `date_ideas` de la pareja A.

Expected: `A recibe su evento` **y** `B NO recibe el canal ajeno`. Los dos, o no hemos probado nada: una policy
con `using (true)` pasaría el primero y fallaría el segundo. Fíjate también en el `status` de B: lo correcto es
que ni siquiera llegue a `SUBSCRIBED`.

**Si `A` no recibe nada:** `realtime.send` desde el trigger no funciona como esperamos. **PARA.** No sigas a la
Task 2. Reporta el `status` y el error exactos: el plan cambia a `realtime.broadcast_changes()` y eso pasa por
brainstorming → writing-plans, no por improvisar.

- [ ] **Step 5: Limpiar y commitear**

Borrar `pw-smoke.mjs`. **Dejar las dos parejas desechables**: la Task 3 las necesita. Anotar sus ids en el ledger.

```bash
git add supabase/migrations/20260708_realtime_couple_broadcast.sql
git commit -m "feat(realtime): per-couple private broadcast from database triggers"
```

---

### Task 2: El componente que escucha

**Files:**
- Create: `src/components/realtime-refresher.tsx`
- Modify: `src/app/(app)/layout.tsx`

**Interfaces:**
- Consumes: `createClient()` de `@/lib/supabase/client` (existe, es `createBrowserClient` de `@supabase/ssr`, y
  **hasta hoy no se usaba en ninguna parte**: esta es la primera vez que la app abre un WebSocket). El canal
  `couple:<id>` de la Task 1.
- Produces: nada que otras tareas consuman.

**Contexto que el brief no puede saber:** `supabase-js@2.110` llama a `realtime.setAuth()` en `SIGNED_IN`,
`TOKEN_REFRESHED` y `SIGNED_OUT`. No hay que gestionar el token del canal privado a mano.

- [ ] **Step 1: Crear el componente**

Crea `src/components/realtime-refresher.tsx`:

```tsx
"use client";

import { useEffect } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";

/**
 * Live sync between the two partners. This app keeps no client-side store — pages
 * are Server Components refreshed by revalidatePath — so Realtime carries no data.
 * It only says "something in your couple changed", and we refetch.
 *
 * One private channel per couple. The RLS policy on realtime.messages is what keeps
 * other couples out; see the migration. Realtime is additive: if the channel never
 * subscribes, the app still works exactly as before, refreshing on navigation.
 */
export function RealtimeRefresher({
  coupleId,
  userId,
}: {
  coupleId: string;
  userId: string;
}) {
  const router = useRouter();

  useEffect(() => {
    const supabase = createClient();
    const channel = supabase.channel(`couple:${coupleId}`, {
      config: { private: true },
    });

    // Deleting a cita cascades to its expenses: one trigger per row. Coalesce them.
    let timer: ReturnType<typeof setTimeout> | null = null;
    const refreshSoon = () => {
      if (timer) clearTimeout(timer);
      timer = setTimeout(() => router.refresh(), 250);
    };

    let firstSubscribe = true;

    channel
      .on("broadcast", { event: "cambio" }, ({ payload }) => {
        // The actor already refreshed through revalidatePath.
        if (payload?.actor === userId) return;
        refreshSoon();
      })
      .subscribe((status, err) => {
        if (status === "SUBSCRIBED") {
          // A reconnect means we missed every event in the gap. Catch up once.
          // Skipped on the first subscribe: the page was just server-rendered.
          if (!firstSubscribe) router.refresh();
          firstSubscribe = false;
        } else if (status === "CHANNEL_ERROR" || status === "TIMED_OUT") {
          console.error("realtime:", status, err);
        }
      });

    return () => {
      if (timer) clearTimeout(timer);
      supabase.removeChannel(channel);
    };
  }, [coupleId, userId, router]);

  return null;
}
```

Las tres piezas que no son opcionales, y por qué:
- **El debounce**: borrar una cita con 5 gastos dispara 6 triggers por la cascada de la FK `expenses.budget_id`.
- **Ignorar los eventos propios**: quien muta ya se refrescó con `revalidatePath`. *(El mediador escribe con el
  cliente service-role, donde `auth.uid()` es `null`; ahí `payload.actor` es `null` y refrescan ambos. Aceptable.)*
- **El `router.refresh()` en el `SUBSCRIBED` que no es el primero**: si el WebSocket se cae y vuelve, los eventos
  del hueco se perdieron para siempre. Sin esto la app se queda callada y desincronizada **sin avisar** — el peor
  fallo posible en una feature cuyo único propósito es no estar desincronizado.

- [ ] **Step 2: Montarlo en el layout autenticado**

`src/app/(app)/layout.tsx` es un Server Component; `RealtimeRefresher` trae su propio `"use client"`, así que
puede renderizarse directamente. Su bloque de imports actual termina en:

```tsx
import { Toaster } from "sileo";
import "sileo/styles.css";
```

Añade **antes** de esas dos líneas:

```tsx
import { RealtimeRefresher } from "@/components/realtime-refresher";
```

El cuerpo actual, tras los dos `redirect()`, es:

```tsx
  const { personA, personB } = derivePartners(ctx);

  return (
    <div className="flex justify-center">
```

`ctx.couple` no puede ser `null` ahí: los dos `redirect()` de arriba devuelven `never`, así que TypeScript ya lo
sabe. Añade el componente como primer hijo del `<div className="flex justify-center">`:

```tsx
  return (
    <div className="flex justify-center">
      <RealtimeRefresher coupleId={ctx.couple.id} userId={ctx.userId} />
```

No cambies nada más del layout: ni el `<Toaster>`, ni el `app-panel`, ni los guards.

- [ ] **Step 3: Verificar que el build pasa**

Run: `NODE_OPTIONS=--use-system-ca corepack pnpm build`
Expected: exit 0, `tsc` limpio, 15 rutas. El build emite `⚠ Compiled with warnings` por `process.version` de
`@supabase/supabase-js` en el Edge runtime: **preexistente y benigno** (`HANDOFF.md §6.8`), no es tu fallo.

Anota el `First Load JS` de `/citas` (antes de esta rama: **160 kB**). El cliente de Realtime entra al bundle por
primera vez; si el aumento parece desproporcionado, dilo.

- [ ] **Step 4: Commit**

```bash
git add src/components/realtime-refresher.tsx "src/app/(app)/layout.tsx"
git commit -m "feat(realtime): refresh the route when the partner changes something"
```

---

### Task 3: Verificación en vivo (la ejecuta el controlador, no un subagente)

**Files:** ninguno. Es verificación, no código.

**Interfaces:**
- Consumes: la migración de la Task 1, el componente de la Task 2, y las dos parejas desechables sembradas en la
  Task 1.
- Produces: la evidencia que autoriza el merge.

**Reglas de entorno:** nunca correr `pnpm build` con `pnpm dev` levantado (sobrescribe `.next` y la app deja de
hidratar), ni `rm -rf .next` en el mismo comando que arranca `pnpm dev` (da `Cannot find the middleware module`).
Playwright: `chromium.launch({ channel: "chrome" })`. Los `pw-*.mjs` van en la raíz y están gitignorados.

Los casos 1 y 3 son **inseparables**: una policy con `using (true)` pasaría el 1 y fallaría el 3. Sin el 3, no se
ha probado nada.

- [ ] **Step 1: Sembrar el segundo miembro de la pareja A**

Esta feature necesita **dos perfiles en la misma pareja**, no uno. Añadir por SQL un segundo usuario confirmado
(`realtime-a2@example.com`) con `couple_id` = el de la pareja A y `partner_role = 'invitado'`.

- [ ] **Step 2: Caso 1 — la feature**

Dos contextos de navegador: A1 y A2, los dos miembros de la pareja A, ambos en `/comunicacion`.
A1 toca un emoji de ánimo. **Sin navegar ni recargar en A2**, el emoji de A1 debe aparecer resaltado en la
pantalla de A2 en un par de segundos.
Expected: A2 se actualiza solo. Eso es la feature entera.

- [ ] **Step 3: Caso 2 — el debounce**

En A1, `/citas`: borrar una cita pasada que tenga al menos 2 gastos (crear los gastos por SQL antes).
Instrumentar A2 contando las peticiones RSC que provoca el refresco:

```js
let refreshes = 0;
pageA2.on("request", (r) => { if (r.headers()["rsc"] === "1") refreshes++; });
```

Expected: A2 se refresca **una vez** (1–2 peticiones RSC), no seis. Si sale 6, el debounce no está funcionando.

- [ ] **Step 4: Caso 3 — la prueba de seguridad**

Un tercer contexto, logueado como el usuario de la **pareja B**, en `/inicio`. Repetir la mutación en A1.
Expected: la pantalla de B **no se refresca**, y su consola no recibe ningún evento. Comprobar además, en el
`status` del canal, que B **no puede suscribirse** al topic de A si se le fuerza a intentarlo.
Si este caso falla, hay una fuga entre parejas y la rama **no se mergea**.

- [ ] **Step 5: Caso 4 — la reconexión**

Con A1 y A2 abiertos: `contextA2.setOffline(true)`. Mutar en A1 (poner un ánimo distinto). Esperar 3 s.
`contextA2.setOffline(false)`.
Expected: cuando el canal vuelve a `SUBSCRIBED`, A2 se pone al día **solo**, sin tocar nada, y muestra el ánimo
que se perdió mientras estaba offline. Sin esto, la app se queda desincronizada en silencio.

- [ ] **Step 6: Limpiar y anotar**

Borrar los tres usuarios y las dos parejas desechables. Comprobar que los recuentos de la BD vuelven exactamente
a los de antes y que la pareja real queda intacta. Borrar los `pw-*.mjs`. Escribir el resultado real —incluidos
los fallos, si los hubo— en `.superpowers/sdd/progress.md`.

---

## Notas para el revisor

- **La policy de `realtime.messages` es el único control de acceso de esta feature.** Antes había 0 policies, así
  que los canales privados denegaban a todos. Si la policy fuera `using (true)`, la feature funcionaría
  perfectamente y filtraría todos los canales a todos los usuarios autenticados. Léela con eso en mente.
- **El `if tg_op = 'DELETE'` de la función trigger no es defensivo, es obligatorio:** en plpgsql, `NEW` no está
  asignado en un trigger de `DELETE` y leer `new.couple_id` lanza. Un `coalesce(new.couple_id, old.couple_id)`
  **no compila el caso DELETE**; parece más limpio y está mal.
- La función es `security definer` con `set search_path = ''`, así que todo va cualificado. Un `jsonb_build_object`
  sin `pg_catalog.` fallaría en ejecución, no en creación.
- **Realtime es aditivo.** Cualquier hallazgo del tipo "si el canal falla el usuario no se entera" es correcto por
  diseño: la app degrada al comportamiento de hoy. Lo que no es aceptable es que un fallo del canal rompa la
  página.
- No se tocan `replica identity` ni la publicación `supabase_realtime`: este diseño no usa el WAL.
