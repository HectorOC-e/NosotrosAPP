auto-install-peers=true
strict-peer-dependencies=false
# Don't auto-run `install` before every `pnpm run/exec` (it fails on the
# optional native build gate for sharp/unrs-resolver).
verify-deps-before-run=false
